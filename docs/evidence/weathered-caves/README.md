# Weathered cave experiment — 2026-09-07

Two seeded spectral-noise studies extend the existing six-room/seven-route plan:
`level-weathered` replaces authored courses; `level-weathered-strata` combines
courses with broad and fine irregularity. This is not hydraulic erosion.

The final local contributor pair is SDK `0.1.0-dev.2574cc89fd30.waves2` and
`runtime-pack-2574cc89fd30-waves5`. Earlier waves3 GPU captures use the same
field/recipe math; later Engine changes concern extraction/admission budgets.

| Normal-detail study | Seed | Triangles | Vertices | Meshing seconds |
|---|---:|---:|---:|---:|
| Weathered | 11 | 119618 | 83756 | 3.866 |
| Weathered strata | 11 | 126574 | 91668 | 3.851 |
| Weathered strata | 29 | 133526 | 95120 | 3.864 |
| Weathered strata | 47 | 126476 | 89616 | 3.822 |

Every listed generation passed 1008/1008 source body clearance witnesses and reported
zero boundary, non-manifold and inconsistent-winding edges. These are not an
exhaustive navigation or geometric-self-intersection certificate.

Root inspected the strata hub/left/goal images: irregular wall relief and
scalloped courses are visible and the branching passage remains readable. The
first `weathered-hub` capture looks indistinguishable from baseline and is not
independent visual proof of the no-courses mode. The blank initial browser image
is explicitly excluded. Reloading Firefox recovered rendering. Labels identify
requested setup; there is no exact game-frame binding or freshness contract.

The native controller moved from (0.778,3.876,31.402) to
(5.181,3.890,25.317) after Tab focus and a 1-second forward stick hold, with blocked=None
in the product readout. Full-loop traversal and frame pacing were not tested.
The original operator index and native action/observation journal are retained;
Moonlight logs/configuration are not copied. Lease release succeeded with no
errors; final status had no lease, sessions or lobbies.

Fine whole-cave extraction first hit the fixed 262,144 triangle cap. The Engine now
exposes extraction budgets. A separate unnecessary vertex-count cutoff was
replaced with validation against the existing 16 MiB resource budget; a 270,000 vertex
mesh with fewer streams passes the focused admission test. The fine cave still
exceeds 16 MiB when encoded, so it is rejected and the previous normal mesh remains.
Engine **#7868** owns partitioned generated resources (and consideration of
asynchronous generation). Fine is not claimed successful.

Repeated scalar probes initially recompiled their JIT tape and caused normal
callback timeouts; the upstream bounded last-tape cache fixes that cost. Normal
weathered presets subsequently ran under the ordinary deadline. The local
authoring manifests now deliberately use `--debugger` for long generation trials;
regeneration is synchronous and this mode removes startup/callback deadlines.

Validation: focused implicit tests, native bridge budget rejection/recovery,
large-mesh admission test, Engine clippy, Procgen checks, UI check and Release
product build passed. This is local worktree evidence, not new CI or release
publication evidence. Concurrent controller edits were preserved.

`final-engine-checkpoint.json` uses the Engine warning script's drain helper:
complete drain, 4 warnings, 0 errors, 0 dropped diagnostic events. It records a stale
control-binding rejection and dropped timing observations during reconnect/load.
These limit telemetry claims; no clean browser-warning delta is claimed.
Engine **#7865** tracks binding/reconnect attribution; **#7868** includes
long-generation lifecycle work. Earlier failed cap readouts remain as evidence.
